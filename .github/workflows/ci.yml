name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    name: Test & Analyze
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      
      - name: Get dependencies
        run: flutter pub get
      
      - name: Build runner
        run: flutter pub run build_runner build --delete-conflicting-outputs
      
      - name: Format generated code
        run: |
          dart format lib test
          if [ -n "$(git status --porcelain -- lib test)" ]; then
            echo "Formatting changes were applied in lib/test. Commit and push them.";
            git --no-pager diff --name-only -- lib test;
            exit 1;
          fi
      
      - name: Analyze code
        run: flutter analyze
      
      - name: Format check
        run: |
          dart format lib test
          if [ -n "$(git status --porcelain -- lib test)" ]; then
            echo "Code style errors: run 'dart format lib test' and commit the changes.";
            git --no-pager diff --name-only -- lib test;
            exit 1;
          fi
      
      - name: Run tests
        run: flutter test
  
  build-android:
    name: Build APK
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      
      - name: Get dependencies
        run: flutter pub get
      
      - name: Build runner
        run: flutter pub run build_runner build --delete-conflicting-outputs
      
      - name: Format generated code
        run: |
          dart format lib test
          if [ -n "$(git status --porcelain -- lib test)" ]; then
            echo "Formatting changes were applied in lib/test. Commit and push them.";
            git --no-pager diff --name-only -- lib test;
            exit 1;
          fi
      
      - name: Build APK
        run: flutter build apk --release
      
      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: build/app/outputs/flutter-apk/app-release.apk

  code-quality:
    name: Code Quality Check
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      
      - name: Get dependencies
        run: flutter pub get
      
      - name: Check for unused imports
        run: dart fix --dry-run
      
      - name: Lint warnings
        run: flutter analyze --no-congratulate